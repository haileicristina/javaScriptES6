/*const arr =[1,2,3,4,5];
arr.forEach((value, index) =>{
    console.log(`${index}: ${value}`);

});*/
const mercado =["sorvete", "bolo", "chocolate"];


const valor = mercado.map((index, compras,) => `${index} - ${compras}` );
console. log(valor);